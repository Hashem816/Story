# سجل التغييرات (Changelog)

## [v2.3.0] - 2026-02-06

### 🔴 إصلاحات حرجة
- ✅ إصلاح زر "آخر 10 مستخدمين" - كان معطلاً تماماً
- ✅ إصلاح زر "قائمة المحظورين" - لم يكن يعمل
- ✅ إصلاح زر "طلبات المستخدم" - لم يكن مرتبطاً بـ handler
- ✅ إصلاح نظام البحث - كان محدوداً جداً (ID و Username فقط)
- ✅ إصلاح Pagination - لم يكن موجوداً
- ✅ إصلاح middleware الصلاحيات - كانت الحماية غير دقيقة

### 🆕 ميزات جديدة

#### نظام اللغات 🌐
- دعم العربية والإنجليزية
- اختيار اللغة عند التسجيل
- إمكانية تغيير اللغة في أي وقت
- جميع النصوص والأزرار متعددة اللغات
- نظام ترجمة مركزي في `utils/translations.py`

#### نظام الكوبونات 🎟️
- إنشاء كوبونات خصم (نسبة مئوية أو مبلغ ثابت)
- تحديد عدد الاستخدامات (محدود أو غير محدود)
- تحديد الحد الأدنى للطلب
- تاريخ انتهاء تلقائي (30 يوم)
- تفعيل/تعطيل الكوبونات
- حذف الكوبونات
- عرض قائمة الكوبونات مع الإحصائيات
- تتبع الاستخدام لكل كوبون

#### سجل العمليات (Audit Log) 📋
- تسجيل تلقائي لجميع العمليات الإدارية
- عرض آخر 20 عملية
- تفاصيل كل عملية (الأدمن، الإجراء، التفاصيل، الوقت)
- إحصائيات العمليات (أكثر العمليات، أكثر الأدمن نشاطاً)
- تسجيل محاولات الوصول غير المصرح بها

#### نظام الإشعارات الموحد 🔔
- إشعارات المستخدمين (تغيير حالة الطلب، تحديث الرصيد)
- إشعارات الأدمن (طلبات جديدة، أخطاء، نشاط مشبوه)
- نظام مركزي في `utils/notifications.py`
- معالجة الأخطاء (حظر البوت، رسائل خاطئة)
- تسجيل الإشعارات الفاشلة

#### تحسينات البث الجماعي 📢
- إرسال على دفعات (batches) بدلاً من واحد تلو الآخر
- تقدم حي أثناء الإرسال (progress bar)
- حفظ سجل البث في قاعدة البيانات
- فلترة المستخدمين النشطين فقط
- تقرير مفصل بالنتائج (نجح/فشل)
- دعم اللغات المتعددة

### 🔧 تحسينات

#### نظام البحث المتقدم 🔍
- البحث بـ Telegram ID
- البحث بـ Username
- البحث بالاسم الأول (first_name)
- البحث بالاسم الأخير (last_name)
- فهرسة لتحسين الأداء
- عرض النتائج مع Pagination

#### إدارة المستخدمين المحسّنة 👥
- عرض جميع المستخدمين مع Pagination
- عرض آخر 10 مستخدمين
- عرض قائمة المحظورين
- عرض طلبات أي مستخدم
- عرض تفاصيل كاملة (الرصيد، عدد الطلبات، تاريخ الانضمام)
- تحسين واجهة المستخدم

#### الأمان 🔐
- تسجيل محاولات الوصول غير المصرح بها
- حماية محسّنة للإجراءات الحساسة
- تحديث قائمة الإجراءات المحمية
- تسجيل تفصيلي في Audit Log
- دعم first_name و last_name في إنشاء المستخدمين

#### قاعدة البيانات 🗄️
- إضافة جدول `coupons` (الكوبونات)
- إضافة جدول `coupon_usage` (استخدام الكوبونات)
- إضافة جدول `audit_logs` (سجل العمليات)
- إضافة جدول `broadcast_history` (سجل البث)
- إضافة جدول `rate_limits` (تحديد معدل الطلبات)
- إضافة جدول `admin_sessions` (جلسات الأدمن)
- إضافة أعمدة `first_name`, `last_name`, `language` في جدول `users`
- إضافة فهرس للبحث السريع في جدول `users`

#### دوال قاعدة البيانات الجديدة
- `search_users()` - بحث متقدم
- `get_users_paginated()` - جلب مع Pagination
- `get_user_orders_count()` - عدد طلبات المستخدم
- `get_user_orders()` - جلب طلبات المستخدم
- `update_user_language()` - تحديث اللغة
- `create_coupon()` - إنشاء كوبون
- `get_coupon()` - جلب كوبون
- `validate_coupon()` - التحقق من الكوبون
- `use_coupon()` - استخدام الكوبون
- `get_all_coupons()` - جلب جميع الكوبونات
- `delete_coupon()` - حذف كوبون
- `log_admin_action()` - تسجيل عملية إدارية
- `get_audit_logs()` - جلب سجل العمليات
- `save_broadcast()` - حفظ سجل البث
- `get_broadcast_history()` - جلب سجل البث
- `check_rate_limit()` - التحقق من معدل الطلبات

### 📁 ملفات جديدة
- `utils/translations.py` - نظام الترجمة
- `utils/notifications.py` - نظام الإشعارات
- `handlers/admin_coupons.py` - إدارة الكوبونات
- `handlers/admin_audit.py` - سجل العمليات
- `handlers/language.py` - اختيار اللغة
- `DIAGNOSIS_REPORT.md` - تقرير التشخيص
- `README_UPDATED.md` - README محدّث
- `CHANGELOG.md` - هذا الملف

### 🎨 تحسينات UX
- أزرار رجوع موحدة في جميع القوائم
- رسائل خطأ واضحة ومفصلة
- رسائل نجاح واضحة
- تأكيد للعمليات الحساسة (حذف، حظر، إلخ)
- تقدم حي للعمليات الطويلة (البث الجماعي)
- عرض الإحصائيات بشكل منظم
- توحيد شكل القوائم

### 🔄 تحسينات الكود
- إضافة تعليقات واضحة
- تنظيم الكود بشكل أفضل
- فصل المسؤوليات (Separation of Concerns)
- استخدام دوال مساعدة
- معالجة أفضل للأخطاء
- تسجيل (logging) محسّن

### 📊 الإحصائيات المحسّنة
- إحصائيات المستخدمين (إجمالي، محظورين، جدد اليوم)
- إحصائيات مالية (إجمالي الأرصدة، إيرادات اليوم)
- إحصائيات الطلبات (يومية، إجمالية، يدوية، تلقائية)
- إحصائيات سجل العمليات

### 🐛 إصلاحات أخرى
- إصلاح خطأ في `admin_user_toggle_` callback
- إصلاح خطأ في `admin_users` protection (كان لا يطابق `admin_users_manage`)
- إصلاح missing handler لـ `admin_user_orders_`
- إصلاح missing handler لـ `admin_user_recent`
- إصلاح missing handler لـ `admin_user_blocked_list`
- إصلاح خطأ في `create_user()` - لم يكن يدعم first_name و last_name

### ⚠️ Breaking Changes
لا توجد تغييرات كاسرة - جميع الميزات الحالية تعمل كما هي.

### 📝 ملاحظات الترقية
- يتم إنشاء الجداول الجديدة تلقائياً عند التشغيل
- يتم إضافة الأعمدة الجديدة تلقائياً (first_name, last_name, language)
- لا حاجة لحذف قاعدة البيانات القديمة
- جميع البيانات القديمة محفوظة

### 🔜 قادم قريباً
- [ ] كلمة سر لوحة الأدمن
- [ ] نظام جلسات الأدمن
- [ ] Rate limiting فعّال
- [ ] تصدير الإحصائيات إلى Excel
- [ ] نظام الإشعارات Push
- [ ] دعم لغات إضافية
- [ ] نظام النقاط والمكافآت
- [ ] تكامل مع APIs إضافية

---

## [v2.2.0] - قبل التحديث

### الميزات الأساسية
- نظام المستخدمين والرتب
- إدارة المنتجات والأقسام
- نظام الطلبات
- نظام الدفع
- الإحصائيات الأساسية
- البث الجماعي البسيط

### المشاكل المعروفة
- بعض أزرار الأدمن لا تعمل
- نظام البحث محدود
- لا يوجد Pagination
- لا يوجد نظام لغات
- لا يوجد نظام كوبونات
- لا يوجد Audit Log

---

**ملاحظة**: هذا التحديث يركز على إصلاح جميع المشاكل المعروفة وإضافة ميزات احترافية جديدة مع الحفاظ على التوافق الكامل مع الإصدارات السابقة.
